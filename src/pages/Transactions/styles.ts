import styled, {css} from "styled-components";

export const  TransactionContainer = styled.main`
  ${({theme}) => css`
    width: 100%;
    max-width: 1120px;
    margin: 4rem auto 0;
    padding: 0 1.5rem;
  `}
`

